package com.ecommerce.orderprocessing.enums;

/**
 * Represents the full lifecycle of a customer order.
 */
public enum OrderStatus {
    PENDING,
    PROCESSING,
    SHIPPED,
    DELIVERED,
    CANCELLED
}
